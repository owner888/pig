{
    "name": "pig/coding-agent",
    "description": "The coding agent: tools the model runs, and the CLI around them.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.3",
        "ext-json": "*",
        "ext-mbstring": "*",
        "pig/agent-core": "@dev",
        "pig/ai": "@dev",
        "pig/async": "@dev",
        "pig/tui": "@dev"
    },
    "autoload": {
        "psr-4": {
            "Pig\\CodingAgent\\": "src/"
        }
    },
    "suggest": {
        "fd": "find tool — pig downloads it to ~/.pig/tools if missing; brew install fd to use your own",
        "ripgrep": "grep tool — pig downloads it to ~/.pig/tools if missing; brew install ripgrep to use your own"
    }
}
